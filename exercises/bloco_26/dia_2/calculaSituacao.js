const respostaCenario1 = (n) => {
if (n === 'reprovado') {
  console.log(`Ok 🚀`);
} else {
  console.error('Resposta não esperada 🚨');
}}

respostaCenario1('reprova')
module.export = respostaCenario1;